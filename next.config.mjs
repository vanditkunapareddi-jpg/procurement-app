/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactStrictMode: true,
  // Expose source maps in production to debug the hook error
  productionBrowserSourceMaps: true,
};

export default nextConfig;
